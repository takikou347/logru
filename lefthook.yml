pre-commit:
  jobs:
    - name: biome check --write
      glob: "*.{js,jsx,ts,tsx,json,jsonc}"
      run: pnpm biome check --write {staged_files}
      stage_fixed: true

pre-push:
  jobs:
    - name: typecheck
      run: pnpm typecheck
    - name: test
      run: pnpm test
